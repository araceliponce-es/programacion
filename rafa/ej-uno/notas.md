sudo adduser daw2al15 docker


(quizas)
docker context use default

docker compose up -d



----

(con esto pasa a ser root, en lugar de usuario)
docker compose exec php bash





composer install



RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^ index.php [QSA,L]

significa que si ese archivo .php no existe lo nvia a idex.php y el nombre del recurso que el user quiere




quitar proofreading ????